# 项目 9 步实施路线图

## Step 4：建立三大业务域的基础查询、展示与回看层

### 1. 本阶段目标

本阶段的核心目的，是在 Step 3 已经建立统一输入主链的基础上，把系统第一次从“能把数据接住并入库”推进到“**能稳定地把正式数据以业务视角展示出来，并支持基础回看与检索**”的状态。

如果说：

* Step 1 冻结了项目边界与骨架
* Step 2 建立了统一运行核心与数据底座
* Step 3 建立了统一输入主链

那么 Step 4 的作用就是：

**让正式数据第一次具备可被人实际查看、理解、筛选和回顾的业务存在感。**

这一阶段不是为了追求复杂报表、炫酷图谱或完整使用体验，而是为了回答一个非常关键的问题：

> 当统一主链把数据写进正式业务域之后，这些数据能否以稳定、清晰、低心智负担的方式被查看和使用？

这一步在整个 9 步路线中的作用，是建立“**事实层的可见性**”。
没有这一步，系统仍然只是一个会吞数据、会审核、会写库的后台系统；有了这一步，它才开始具备“工作台”的最基本使用价值。

本阶段的重点不是“页面数量”，而是：

1. 正式业务对象能被稳定读取
2. 三大业务域都有基础查询入口
3. 数据展示以业务语义组织，而不是以数据库表组织
4. 回看和检索逻辑建立在正式数据之上，而不是建立在 Capture/Pending 之上
5. 后续 Dashboard、规则、AI、模板模式都将以这里建立的“正式业务视图层”为依托

因此，Step 4 是整个项目从“主链平台”转向“可使用工作台”的第一步，但仍然必须保持克制：
**它做的是基础可见性，不是高级分析。**

---

### 2. 完成后的软件状态

到这个阶段结束时，系统应进入一种**“虽然还不是完整产品，但已经可以围绕正式业务数据进行基本查看、筛选与回顾”**的状态。

已经具备的东西：

* Expense、Knowledge、Health 三大业务域都已有正式数据的基础读取能力。
* 系统可以按业务语义而非底层表结构展示正式记录。
* 用户可以查看正式入库后的消费记录、知识卡片、健康记录。
* 系统可以进行最基础的筛选、分页、排序、时间范围查看等回看操作。
* Capture/Pending 与正式业务层的区别在展示层已明确，不再混淆。
* Knowledge 的“素材层 / 整理层”边界开始在展示上体现出来。
* Health 的“硬指标 / 主观记录”边界开始在展示上体现出来。
* 系统第一次具备“我可以回来看看最近都发生了什么”的基础能力。

还不具备的东西：

* 还没有完整 Dashboard 聚合价值。
* 还没有 AI 派生展示内容。
* 还没有规则提醒与健康告警消费层。
* 还没有 Telegram 正式业务接入。
* 还没有桌面壳承载主工作台体验。
* 还没有模板工作模式的真正价值发挥。
* 还没有知识图谱、复杂趋势图、报表深钻等高级功能。

这是合理的阶段性状态，因为 Step 4 的目的不是“让系统一下子变完整”，而是先让**正式业务层可见、可查、可回顾**。
只有当正式业务数据的可见性建立起来后，后续的 Dashboard、AI、模板、提醒和多入口使用才会有真实承载对象。

---

### 3. 本阶段核心任务

本阶段必须完成的，是三大业务域的基础读取与展示能力，而不是高级分析能力。

#### 任务一：建立正式业务层的统一读取接口

必须完成：

* Expense 的基础读取能力
* Knowledge 的基础读取能力
* Health 的基础读取能力
* 每个业务域都能以正式记录为中心被查询
* 查询逻辑统一走服务层，不允许页面直接拼 SQL 风格行为

关键交付物是：

* 三大业务域的基础读取服务
* 对应的统一查询接口
* 面向正式业务对象的返回结构

这一任务的重点是：
系统第一次开始“以正式业务记录为中心说话”，而不是继续围绕 Capture / Pending 组织一切。

---

#### 任务二：建立基础筛选、排序、分页与时间范围查询能力

必须完成：

* 最小分页能力
* 最小排序能力
* 按时间范围筛选
* 按模块关键字段进行基础筛选

例如但不限于：

* Expense：日期、分类、商户
* Knowledge：主题、状态、创建/更新时间、来源类型
* Health：记录类型、指标类型、时间范围

关键交付物是：

* 统一的基础查询参数规范
* 各模块最小可用筛选集合
* 基础排序与分页能力

这一任务的重要性在于：
如果这一步不先建立，后续页面和 Dashboard 会不断用“特例接口”来绕过统一读取模型。

---

#### 任务三：建立正式业务详情视图的最小形态

本阶段不能只做列表，还必须让每个业务域具备最小详情视角。

必须完成：

* Expense 单条详情查看
* Knowledge 单条详情查看
* Health 单条详情查看

且详情必须体现：

* 正式业务字段
* 来源关系（如来自哪个 Capture / 是否经过 Pending）
* 当前状态
* 与原始输入的最小可追溯性

关键交付物是：

* 三大模块的详情读取能力
* 最小“来源关系”展示字段
* 正式对象的可追溯视图

这一步的意义是：
系统不是只给你看一张表，而是要让“正式事实对象”真正成立。

---

#### 任务四：明确展示层中的业务边界

本阶段必须在展示层中，把几个前面已定稿的边界正式体现出来：

1. **Capture / Pending / 正式业务层分层显示**

   * 不允许把原始输入和正式记录混在同一视图中伪装成一回事

2. **Knowledge 双层结构开始显性化**

   * 原始素材与整理后的知识卡片不能在展示层被混作一个概念

3. **Health 双路径开始显性化**

   * 硬指标记录与主观记录必须在展示上有清晰区分

关键交付物是：

* 不同层级对象的明确视图归属
* 不再混淆层次的页面/接口结构
* 可供后续 AI、规则、提醒挂接的清晰展示基础

这一步很关键，因为展示层一旦把边界做糊，后面用户感知上的系统结构就会被破坏。

---

#### 任务五：建立最小 Web 工作台基础页骨架

这一阶段可以开始正式建立最小业务工作台页面，但仍然必须控制范围。

必须完成的页面级交付物是：

* Expense 基础列表页
* Knowledge 基础列表页
* Health 基础列表页
* 每个模块的基础详情视图
* Capture / Pending 与正式模块在导航和语义上已有明确区分

这里的“页面”不是重点，重点是：

* 页面是否以正式业务层为核心
* 页面是否遵守既定边界
* 页面是否只消费服务层能力，而不自行定义业务逻辑

---

#### 任务六：建立最小回看价值验证

本阶段必须至少证明一件事：

> 系统已经不仅能“接住并入库数据”，还能让你从业务角度回看这些数据。

因此必须完成最小验证样例，例如：

* 能查看某时间段的消费正式记录
* 能查看整理后的知识卡片列表与详情
* 能查看健康硬指标记录与主观记录的基础列表
* 能从正式记录追溯来源 Capture / Pending
* 能通过列表 + 详情完成一次“回看最近发生过什么”的基础操作

这一步不是技术附加项，而是为了证明系统已经开始具备“工作台”属性。

---

### 4. 本阶段不做的事

Step 4 很容易被误做成“顺势把产品前端都铺开”，这会严重打乱节奏。必须明确以下内容现在不做。

#### 不做 Dashboard 聚合与总览中控

现在不能开始展开：

* 首页统计卡片
* 总览趋势图
* 跨模块聚合入口
* 模板入口联动

因为这些属于“正式业务层之上的中控与聚合层”，必须建立在三大模块基础读取已经稳定之上，而不是反过来压着模块定义来做。

---

#### 不做高级分析与图表

现在不能开始做：

* 消费占比图
* 月报/周报图
* 健康趋势图
* 知识关系图谱
* 自动聚类与知识网络

这些能力都属于“消费正式业务层的分析层”，不是本阶段的职责。
本阶段先保证“看清对象”，而不是“把对象可视化得很漂亮”。

---

#### 不做 AI 派生展示

现在不能开始加入：

* 知识摘要展示
* 主观健康 AI 分析展示
* Dashboard 文本总结
* AI 建议标签或关系提示

原因是：
AI 派生层必须建立在“正式业务层视图已经稳定”之上，否则前端很容易先围绕 AI 内容组织页面，污染事实层的中心地位。

---

#### 不做规则消费层

虽然 Health 的规则提醒最终很重要，但现在不能开始做：

* alerts 页面
* 连续异常提醒展示
* 风险等级聚合展示

因为健康规则本身还没进入正式建设阶段，本阶段不能提前把消费层造出来。

---

#### 不做多入口正式接入

现在不能让：

* Telegram
* 桌面壳

正式承担这套业务视图的主入口角色。
当前的重点仍然是先用统一 Web 工作台把正式业务层的可见性建立起来。

---

### 5. 验收标准

Step 4 是否完成，不能以“做了几个页面”来判断，而必须看正式业务层是否真正可见、可回看、可区分边界。

#### 三大业务域基础读取验收

* [ ] Expense 正式记录可列表读取
* [ ] Knowledge 正式记录可列表读取
* [ ] Health 正式记录可列表读取
* [ ] 读取逻辑统一走服务层
* [ ] 不存在前端或路由层拼凑业务逻辑替代正式读取能力

#### 基础查询能力验收

* [ ] 三大业务域均支持最小分页
* [ ] 三大业务域均支持最小排序
* [ ] 三大业务域均支持时间范围筛选
* [ ] 各业务域支持最基础的业务字段筛选
* [ ] 查询参数风格统一，不是每个模块各写一套临时规则

#### 详情视图验收

* [ ] Expense 存在单条详情读取能力
* [ ] Knowledge 存在单条详情读取能力
* [ ] Health 存在单条详情读取能力
* [ ] 详情中可看到正式业务对象的来源关系
* [ ] 正式记录与 Capture / Pending 的追溯关系可检查

#### 展示边界验收

* [ ] Capture / Pending / 正式业务层在界面语义上没有混淆
* [ ] Knowledge 的素材层与知识卡片层未被混作一个对象
* [ ] Health 的硬指标与主观记录有明确区分
* [ ] 页面没有用“方便展示”为理由破坏层次结构

#### 最小工作台页面验收

* [ ] 存在 Expense 基础列表与详情页
* [ ] 存在 Knowledge 基础列表与详情页
* [ ] 存在 Health 基础列表与详情页
* [ ] 页面能够消费真实正式数据，而不是假数据或仅展示 Capture/Pending

#### 回看价值验收

* [ ] 可完成一次“查看最近消费记录”的基础回看
* [ ] 可完成一次“查看近期知识卡片”的基础回看
* [ ] 可完成一次“查看健康正式记录”的基础回看
* [ ] 可从正式记录追溯来源路径

只有当系统已经能围绕三大正式业务域进行**基础查询、基础详情查看、边界清晰的正式数据回看**时，Step 4 才算完成。

---

### 6. 设计理念与架构约束

Step 4 的作用，不是增加“可见的功能数量”，而是让系统真正开始表现出“工作台”的基础形态。这里最容易出错的地方，是为了让界面看起来更丰富，反过来破坏前面刚建立好的主链与边界。

#### 为什么此时做这些

因为前面 Step 3 已经解决了“输入如何变成正式事实”的问题。
现在必须尽快让这些正式事实可以被稳定地看见，否则系统仍然只是一个处理后台，而不是工作台。

对你这个项目来说，“回来看我最近都记录了什么、整理了什么、发生了什么”是非常核心的真实使用场景。
因此在这个阶段，最有价值的不是图表，不是模板，不是 AI，而是：

* 我能看到正式记录
* 我能区分不同层级的数据
* 我能按业务方式回看
* 我能从正式事实反查来源

这正好对应了你定稿里的“中控台 / 工作台 / 长期归档底座”定位。

---

#### 为什么暂时不做另外一些

因为以下内容虽然诱人，但都建立在正式业务层可见性已经稳固之上：

* Dashboard 是正式业务层的上层聚合
* AI 派生是正式业务层的解释增强
* 规则提醒是正式健康记录的后续判断
* 图谱是整理后的知识卡片之上的结构化视图
* 模板是进入特定工作模式的入口组织方式

如果现在提前做这些，就会让系统重心从“正式业务层”滑向“展示炫技层”或“入口组织层”。

---

#### 本阶段最重要的边界和约束

1. **页面只能消费正式业务读取能力，不能反向定义业务模型**

   * 页面不是数据设计者

2. **本阶段的中心是正式业务对象，不是 Capture/Pending**

   * Capture/Pending 仍然重要，但此阶段重点是正式层可见性

3. **展示必须显式保留层次感**

   * 不允许为了界面简洁把原始层、审核层、正式层混成一个概念

4. **Knowledge 与 Health 的双层/双路径边界必须开始在用户感知中成立**

   * 这不是内部实现细节，而是系统结构本身的一部分

5. **只做基础查询与回看，不做高级分析与派生消费**

   * 保持节奏克制，避免页面先于系统中心膨胀

---

#### 本阶段如何保证后续不偏离初心

Step 4 保证不偏离初心的方式，是把“正式事实层是系统中心”这件事第一次落实到用户可见界面中。

这意味着后续：

* Dashboard 是在正式事实层之上聚合，而不是替代它
* AI 派生是附着在正式事实层之上，而不是盖住它
* Health 提醒是消费正式记录，而不是另起一套系统
* 模板工作模式是围绕这些正式视图组织，而不是取代它们
* Bot 与桌面壳接入时，也应围绕同一批正式业务对象展开

也就是说，Step 4 是后续所有“更像产品”的能力之前的关键防偏航步骤。

---

### 7. 常见偏航风险

Step 4 最容易犯的错误，是把“正式业务层可见性建设”误做成“前端铺页面”。

#### 风险一：为了快，直接把 Capture/Pending 当成正式业务列表来展示

这会让系统层次在用户感知上彻底混掉。
后面再补正式业务层，会非常难解释系统到底哪些是原始输入、哪些是正式事实。

---

#### 风险二：页面先行，接口和服务层被动适配

常见形式是：“页面要这样显示，所以临时加个接口返回这个拼装数据”。
这会导致服务层和业务对象被前端反向塑形，后续越做越碎。

---

#### 风险三：过早追求图表、关系图、总览卡片

这些内容很容易带来“成就感”，但如果正式业务对象的基础查询与详情还不稳，就会让系统表面很丰富，底层却很虚。

---

#### 风险四：把 Knowledge 和 Health 的结构边界在展示层做糊

例如：

* 把 knowledge source 和 knowledge card 混成一个列表对象
* 把 health metric 和 subjective record 混成“健康记录”一个概念

这会直接破坏后续图谱、AI 分析、规则提醒的可挂接性。

---

#### 风险五：为了展示方便，引入 AI 文本或自动解释

这会让用户太早把系统的关注点放到“模型说了什么”，而不是“正式事实是什么”。
对于你的项目定位来说，这是明显偏航。

---

#### 风险六：把 Step 4 做成“可用前端 MVP”，而不是“正式业务层可见性建设”

这两者看起来相近，实际差别很大。
如果你追求的是“前端能看”，就容易做很多绕过主线的临时拼接；
如果你追求的是“正式业务层可见”，就会把中心放在对象和边界上。

---

### 8. 进入下一步的前提条件

只有在以下条件全部满足时，才适合进入 Step 5：

1. **三大正式业务域已经具备稳定、统一的基础查询能力**

   * 不是临时页面拼出来的结果

2. **正式对象详情视图已经成立**

   * 能从业务角度理解正式记录，而不是只看到表字段

3. **用户已能完成最基础的正式数据回看**

   * 系统开始具备“工作台”的最初使用价值

4. **展示层没有破坏前面建立的边界**

   * Capture / Pending / 正式层，Knowledge 双层，Health 双路径都仍然清晰

5. **尚未被 Dashboard、AI、图表、Bot、模板等更上层能力抢走节奏**

   * 也就是 Step 4 仍然保持了“先对象可见，后上层消费”的克制性

下一步依赖这一阶段沉淀下来的核心成果是：

* 三大业务域稳定的基础读取模型
* 正式业务对象的详情与来源追溯能力
* 明确的用户可见层级结构
* 可供上层聚合消费的正式业务视图基础

只有这些都成立了，Step 5 才适合开始建设**Dashboard 总览、跨模块聚合与工作台中控层**。
