# 项目 9 步实施路线图

## Step 2：建立统一运行核心与数据底座

### 1. 本阶段目标

本阶段的核心目的，是在 Step 1 已经冻结的边界和工程主骨架之上，建立这个项目真正可持续运行的**统一应用核心**。如果说 Step 1 解决的是“项目不能再漂移”，那么 Step 2 解决的就是“项目不能再散着长”。

这一阶段在整个 9 步路线中的作用，是把后续所有功能都会依赖的底层运行能力先立住，主要包括三类：

1. **统一运行入口**
   让系统从“有工程骨架”进入“有统一应用核心”的状态。后续 Web、桌面壳、Bot、任务调度都要围绕这个核心展开，而不是各自长出一套逻辑。

2. **统一数据底座**
   把 SQLite 作为唯一真相源真正落成正式数据系统，而不是停留在原则层。这里不是去做业务表全量实现，而是先把“数据库如何存在、如何初始化、如何迁移、如何访问、如何隔离原始层/正式层/派生层”这些底层规则立稳。

3. **统一基础设施能力**
   包括配置加载、环境区分、日志、异常处理、健康检查、基础任务运行框架等。这些能力看起来不像业务功能，但它们决定后面系统会不会变成一堆能跑但不稳的模块。

这一阶段不是在“做业务”，而是在建立一个后续所有业务都必须依赖的**长期底座**。
它的定位是：**让项目从“方向正确的空壳”进入“结构稳定、可承载业务的应用核心”状态。**

---

### 2. 完成后的软件状态

到这个阶段结束时，系统应进入一种**“虽然业务功能仍然很薄，但底层运行方式已经稳定”**的状态。

已经具备的东西：

* 项目有统一的应用服务入口，不再只是一个空仓库结构。
* SQLite 已被正式接入为系统唯一真相源，而不是“以后再接数据库”。
* 数据访问方式、初始化方式、迁移方式、会话边界已经确定。
* 环境配置、业务配置、规则配置、模板配置的分类方式已经落地。
* 系统有最基础的日志、错误处理、健康检查与运行状态能力。
* 可以存在最小可调用的系统级接口，例如系统状态、版本、配置加载状态、服务健康检查等。
* 后续模块开发已经不再需要反复重建底层能力，而是可以直接长在统一运行核心之上。

还不具备的东西：

* 还没有正式的 Capture 主链业务闭环。
* 还没有 Pending 审核功能。
* 还没有 Expense / Knowledge / Health 的可用业务功能。
* 还没有真实 AI 派生结果。
* 还没有 Telegram 闭环。
* 还没有桌面壳接入真实业务。
* 还没有 Dashboard 的业务聚合价值。

这是合理的阶段性状态，因为 Step 2 的职责不是“让用户开始用”，而是确保后续任何业务功能都不是搭在临时脚手架上。
此时系统应该像一个“已经打好地基和承重结构的建筑”，而不是“外观先画出来的样板间”。

---

### 3. 本阶段核心任务

本阶段必须完成的任务，是把“统一应用核心”和“统一数据底座”正式建起来。重点不是模块数量，而是底层稳定性。

#### 任务一：建立统一应用服务入口

必须明确并落地：

* 系统的主服务入口是什么
* 应用启动时做哪些初始化
* 子模块如何注册到统一服务中
* 系统级生命周期由谁负责
* 后续入口（Web、桌面壳、Bot、任务调度）如何围绕它接入

关键交付物是：

* 统一的应用启动入口
* 明确的初始化流程
* 明确的模块注册机制
* 系统级健康检查与版本信息接口

这里的重点是“统一入口”，而不是“先把所有路由都写出来”。

---

#### 任务二：正式建立数据库底座

必须完成：

* SQLite 的正式接入
* 数据库连接与会话管理方式固定
* 模型基类、时间字段、主键策略、通用字段约定固定
* 数据库初始化机制建立
* 数据迁移策略确定并落地最小可用版本

关键交付物是：

* 正式数据库初始化脚本或机制
* 模型基类和数据库访问基础层
* 可验证的数据库可创建、可连接、可管理状态
* 迁移工具或迁移规范已经建立，而不是以后补

这一任务必须特别强调：
不是现在把所有业务表全部做完，而是先把“表将来怎么进入系统、怎么变更、怎么被访问”这个底层通道建立起来。

---

#### 任务三：落地配置系统与环境边界

必须建立统一配置加载与分类机制，至少明确并落地：

* 环境变量配置
* 应用运行配置
* 数据库配置
* 日志配置
* 后续规则、模板、模型参数配置的归属位置

关键交付物是：

* 配置加载入口
* 配置分类与读取方式
* `.env`/应用配置/业务配置的责任边界
* 开发环境与正式运行环境的最小区分

这里的目标不是做复杂配置平台，而是**从一开始就避免配置散落在代码里**。

---

#### 任务四：建立统一日志、异常与错误返回规范

必须完成：

* 统一日志输出格式
* 应用级异常处理
* 接口级错误结构
* 关键错误分级原则
* 后续任务与模型调用失败的基础承载方式

关键交付物是：

* 日志初始化逻辑
* 统一错误返回结构
* 应用内异常处理入口
* 至少一种明确的“错误不是 silently fail”的机制

这一任务的重要性在于：
如果现在不做，后面业务越多，调试成本会指数级上升。

---

#### 任务五：建立最小后台任务运行框架

这一阶段不要求实现真实 AI 任务、周报任务或复杂调度，但必须把“后台任务会存在”这件事的底层承载点先立住。

必须完成：

* 后台任务的最小运行框架
* 同步请求与异步任务的基本边界
* 任务状态最小可见性原则
* 后续定时任务与慢任务的落点预留

关键交付物是：

* 最小任务执行入口或占位能力
* 明确哪些能力以后不应塞进同步请求中
* 不再允许“以后再看慢任务怎么处理”

这一点的作用，是防止后续 AI、导出、规则巡检、周报生成一股脑塞进接口请求里。

---

#### 任务六：建立基础工程质量护栏

至少要落地：

* 代码格式化与检查基线
* 基础测试运行能力
* 最小 smoke test 或启动验证
* 文档与代码基线同步要求

关键交付物是：

* 能运行的最小测试/检查命令
* 明确哪些基础文件不可随意绕过
* 后续 Codex 可被要求“改完必须通过”的最低质量门槛

这一步不是为了“工程洁癖”，而是为了后面每加一步，不会把整个系统越写越不可控。

---

### 4. 本阶段不做的事

Step 2 仍然必须克制，不允许因为底座已经初步成立，就开始“顺便做业务”。

#### 不做业务主链

现在不能开始真正实现：

* Capture 录入闭环
* Parse 分发逻辑
* Pending 审核状态流转
* confirm / discard / fix / force-insert

因为这些属于下一阶段要正式建立的主链业务，不应在 Step 2 期间被半实现。

---

#### 不做业务模块页面

现在不能去做：

* Expense 页面
* Knowledge 列表页
* Health 页面
* Dashboard 展示页
* Templates 应用页

即使前端空壳可以存在，也不能在这一阶段开始做业务页面为主的推进。
否则底层运行核心很容易反过来被页面需要牵着走。

---

#### 不做 AI 能力接入

现在不能接：

* 知识摘要
* 主观健康分析
* Dashboard 文本总结
* 模型调用封装的完整实现

因为 AI 接入必须建立在业务对象和任务边界已经清楚的前提上，现在时机还早。

---

#### 不做健康规则与提醒

虽然规则引擎最终很重要，但当前不能开始做真实规则判断。
否则会在业务对象、任务流、告警状态都未稳固时，提前引入复杂判断逻辑。

---

#### 不做 Telegram 与桌面壳的正式接入

这两者当前都只能保留占位或最小入口，不应开始做真实交互闭环。
因为此时服务核心尚未承载正式业务流，过早接入只会逼迫底层妥协。

---

### 5. 验收标准

Step 2 的完成必须可检查、可验证、可重复，而不是“感觉底层差不多了”。

#### 应用服务入口验收

* [ ] 存在统一应用启动入口
* [ ] 应用可完成最小启动流程
* [ ] 存在系统级健康检查或状态检查接口
* [ ] 模块注册或初始化流程有固定位置，不再临时拼接

#### 数据库底座验收

* [ ] SQLite 已被正式接入
* [ ] 数据库初始化流程可执行
* [ ] 至少存在正式的模型基类与会话管理机制
* [ ] 数据库可成功创建最小基础表
* [ ] 数据迁移机制或最小迁移规范已建立，而不是“以后再补”

#### 配置系统验收

* [ ] 环境配置加载可运行
* [ ] 应用配置分类已落地
* [ ] 不同类型配置的归属有明确位置
* [ ] 不再依赖临时硬编码来驱动应用启动

#### 日志与异常验收

* [ ] 应用启动日志可见
* [ ] 错误返回结构已统一
* [ ] 存在统一异常处理入口
* [ ] 关键失败不会静默吞掉

#### 后台任务框架验收

* [ ] 已存在最小任务执行或调度承载点
* [ ] 同步请求与异步任务的边界原则已有正式表达
* [ ] 后续慢任务不再需要重新发明机制

#### 工程质量护栏验收

* [ ] 项目至少可以运行最小检查或测试命令
* [ ] 存在基础格式化/检查约束
* [ ] 后续模块开发有最低质量门槛可依赖

只有当系统已经形成“统一应用入口 + 正式数据库底座 + 配置系统 + 日志异常规范 + 后台任务最小框架 + 工程质量护栏”时，Step 2 才算完成。

---

### 6. 设计理念与架构约束

Step 2 的核心不是“把框架搭起来”，而是**把后续所有功能都无法轻易绕开的底层约束正式落地**。

#### 为什么此时做这些

因为前面已经定稿，这个项目最看重的是：

* 长期可维护
* 统一主线
* 平台底座优先
* 不因短期方便破坏结构
* AI 和规则职责清晰
* 多入口共享而不分裂

这些要求都意味着，数据库、配置、任务、日志、统一服务入口这些东西不能晚做。
晚做的结果不是“以后补一下”，而是后续每个模块都已经各自长出自己的依赖方式，很难统一。

---

#### 为什么暂时不做另外一些

因为当前阶段的目标是“建立承重结构”，而不是“装修房间”。

* 不做业务主链，是因为主链必须建立在统一服务入口与数据库底座之上。
* 不做页面，是因为页面最容易反向绑架数据与服务结构。
* 不做 AI，是因为 AI 一旦早接，很容易为了效果牺牲边界。
* 不做 Bot 与桌面壳正式接入，是因为它们都应共享同一个服务核心，而不是倒逼核心结构。

---

#### 本阶段最重要的边界和约束

这一阶段必须坚持以下硬约束：

1. **底层能力先成正式机制，再允许业务依赖**
   不能通过“先在模块里手写一版，后面再抽出来”的方式推进。

2. **数据库底座必须是统一入口，不允许模块私自定义自己的访问模式**
   否则后面每个模块都会长出一套不同的数据风格。

3. **配置必须被视为正式系统组成，而不是启动细节**
   否则未来规则、模板、模型参数都会失控。

4. **后台任务必须先有承载原则，不能等 AI 和导出上来后再补**
   否则慢任务会污染请求链和模块逻辑。

5. **日志与异常处理必须统一，否则一切“看起来能跑”的功能都不可维护**
   这不是后补项。

---

#### 本阶段如何保证后续不偏离初心

Step 2 保证不偏离初心的方式，是让后续所有能力都必须通过：

* 统一应用核心
* 统一数据库底座
* 统一配置边界
* 统一异常与日志规范
* 统一任务执行边界

这样后续无论做 Capture、Pending、Expense、Knowledge、Health、AI、Templates、Bot，都会自然长在同一套平台逻辑中，而不是边做边偏。

---

### 7. 常见偏航风险

Step 2 最容易犯的错误，是把“底层搭建”做成了要么过度设计、要么偷懒跳过。

#### 风险一：用临时数据库访问方式顶替正式底座

表面上看先能跑，实际上后面每个模块都可能依赖不同的 session、连接、模型习惯。
这会直接导致后续模块越多，重构越痛。

---

#### 风险二：把配置继续写死在代码里

这通常会以“先写个默认值”“这个路径先写死”“这个环境变量以后再整理”的形式出现。
短期看推进快，长期则会让规则、模板、模型、环境全都混乱。

---

#### 风险三：为了“先能用”把业务逻辑塞进启动层或入口层

例如在应用入口里顺便写判断、在初始化流程里顺便写业务逻辑、在健康检查里塞模块行为。
这样会导致系统主骨架从一开始就混杂。

---

#### 风险四：提前做业务模块的“半成品”

比如先建 Expense API、先做 Capture 接口、先做一个 Knowledge 表。
这看起来像推进，实际上是在未经完整主链约束前，把数据和服务结构提前固化。

---

#### 风险五：后台任务框架被忽略

开发时最常见的想法是“先同步跑，以后再拆”。
这对普通小项目可能还能忍，但对你这个一定会有 AI、导出、巡检和聚合任务的系统来说，后面代价会很高。

---

#### 风险六：把 Step 2 误做成“框架炫技”

另一个极端是过度工程化，例如：

* 过早做复杂插件系统
* 过早做通用事件总线
* 过早做高度抽象的中台层

这会让底层看起来很完整，但实际上脱离当前项目真正需要的统一主线。

---

### 8. 进入下一步的前提条件

只有在以下条件都满足后，才适合进入 Step 3：

1. **统一应用服务入口已经稳定存在**
   后续业务不再需要重新定义“系统核心从哪里运行”。

2. **SQLite 数据底座已经正式建立**
   包括初始化、访问、会话、迁移基础机制都已固定。

3. **配置、日志、异常处理已经形成统一约定并可运行**
   后续模块不再需要各自定义一套基础设施。

4. **最小后台任务承载点已经存在**
   后续不会被迫把慢任务塞进同步请求。

5. **还没有任何正式业务功能提前把底层结构绑死**
   也就是 Step 2 仍然保持了“先骨架，后主链”的节奏。

下一步依赖这一阶段沉淀下来的核心成果是：

* 统一应用核心
* 正式数据库底座
* 配置与环境边界
* 日志与错误处理规范
* 后台任务最小承载框架
* 工程质量最低护栏

只有这些都稳了，Step 3 才适合开始建立真正贯穿全系统的**Capture → Parse → Pending → Confirm** 主链。
